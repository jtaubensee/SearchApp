<?xml version="1.0" encoding="utf-8"?>
<pages:iOSSearchPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:SearchApp"
    xmlns:pages="clr-namespace:SearchApp.Components.Pages"
    xmlns:controls="clr-namespace:SearchApp.Components.Controls"
    x:Class="SearchApp.MainPage"
    x:Name="MainSearchPage"
    Title="Main"
    SearchPlaceholder="Search Text"
    ActionImage="barcode_black_24">
    <pages:iOSSearchPage.Content>
        <controls:SearchView SearchEntry="{x:Reference MainSearchPage}">
            <controls:SearchView.PrimaryView>
                <Label
                    Text="Primary View"
                    HorizontalOptions="Center"
                    VerticalOptions="CenterAndExpand" />
            </controls:SearchView.PrimaryView>
            <controls:SearchView.ResultsView>
                <Label
                    Text="Results View"
                    HorizontalOptions="Center"
                    VerticalOptions="CenterAndExpand" />
            </controls:SearchView.ResultsView>
        </controls:SearchView>
    </pages:iOSSearchPage.Content>
</pages:iOSSearchPage>